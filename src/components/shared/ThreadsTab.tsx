type Props = {
  currentUserId: string;
  accountId: string;
  accountTypeId: string;
};

const ThreadsTab = ({ currentUserId, accountId, accountTypeId }: Props) => {
  // TODO: Fetch Profile Threads
  return <div>ThreadsTab</div>;
};

export default ThreadsTab;
